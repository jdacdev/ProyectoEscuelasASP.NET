namespace HolaMundoMVC.Models
{
    public enum TiposEscuela
    {
        // se puede asignar una numeracion a los elementos (es equivalente a una lista de enteros)
        // pero en caso de no ponerlos estos son tomado por defecto en el orden que esta (1,2,3)
        // Preparatoria = 0
        Primaria,
        Secundaria,
        Preescolar

    }
}